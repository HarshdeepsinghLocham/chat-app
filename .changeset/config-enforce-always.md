---
"@semantask/services": patch
"@semantask/task-worker": patch
---

Always enforce execution mode. `EXECUTION_MODE_ENFORCE=0` is ignored (one-time warning). The env parser stays until a later delete PR.

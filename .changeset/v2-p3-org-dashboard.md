---
"@semantask/types": minor
"@semantask/services": minor
"@semantask/web": patch
---

Phase 3 PR3 — Feature-flagged org coordination dashboard (`ORG_DASHBOARD` default off) with `GET /api/organizations/:id/work-summary` and `/inbox/dashboard` widgets for open work, aging approvals, and pending high-risk tools.
